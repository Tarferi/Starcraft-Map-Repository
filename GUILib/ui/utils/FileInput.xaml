<UserControl x:Class="GUILib.ui.utils.FileInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUILib.ui.utils"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <d:DesignerProperties.DesignStyle>
        <Style TargetType="UserControl">
            <Setter Property="Background" Value="White" />
            <Setter Property="Height" Value="500" />
            <Setter Property="Width" Value="500" />
        </Style>
    </d:DesignerProperties.DesignStyle>
    <Grid>
        <TextBox Name="txt" Margin="0,0,35,0" VerticalContentAlignment="Center" MouseLeftButtonDown="txt_MouseLeftButtonDown" TextChanged="txt_TextChanged" />
        <Button Name="btn" Content="..." HorizontalAlignment="Right" Width="33" Click="btn_Click"></Button>
    </Grid>
</UserControl>
